# 🚀 Smart Study Assistant - تشغيل سريع

## خطوات التشغيل:

### المطلوب قبل البدء:
- Node.js مثبت
- npm مثبت

### تشغيل البِاك‌إند (Terminal 1):
```bash
. c:\Users\a\SmartStudyAssistant\start-backend.ps1
```

أو استخدم ملف batch:
```bash
c:\Users\a\SmartStudyAssistant\start-backend.bat
```

### تشغيل الـ Frontend (Terminal 2):
```bash
. c:\Users\a\SmartStudyAssistant\start-frontend.ps1
```

أو استخدم ملف batch:
```bash
c:\Users\a\SmartStudyAssistant\start-frontend.bat
```

---

## البيانات:
- جميع البيانات تُحفظ في ملفات CSV تحت المجلد `backend/data/`
- ملفات الـ Uploads تُحفظ في `backend/uploads/`

## الروابط:
- Backend API: `http://localhost:5000`
- Frontend: `http://localhost:3000`

---

## ملاحظات:
- لا يوجد اتصال بقاعدة بيانات خارجية
- جميع البيانات محفوظة محلياً
- الـ Gemini API يحتاج مفتاح عامل (موجود في `.env`)

---

✅ **كل شيء جاهز!**
